name: carnometer_core
description: Core domain and tracking engine for the Carnometer PoC.
version: 0.1.0
publish_to: none

environment:
  sdk: ^3.11.0

dev_dependencies:
  lints: ^6.0.0
  test: ^1.26.0
